% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{message_parse}
\alias{message_parse}
\title{Report how many features and master proteins remain}
\usage{
message_parse(x, column, note)
}
\arguments{
\item{x}{\code{data.frame} or \code{DataFrame}. Feature-level annotations, normally
the output of \code{rowData()} on an assay.}

\item{column}{\code{string}. Name of the column in \code{x} holding the master protein
accession, e.g. \code{"Master.Protein.Accessions"}.}

\item{note}{\code{string}. Short description of the step being reported.}
}
\value{
Invisibly \code{NULL}; called for the message it prints.
}
\description{
Prints the number of rows and the number of distinct master
proteins in a feature-level annotation table, followed by a short note
saying which step the count describes. The \verb{filter_features_*} functions call this
after each filter they apply, so the same message format can be used to
report a count at a point where no filter function ran, such as after
\code{filterNA()} or a manual subset.
}
\examples{
tmt_qf <- QFeatures::readQFeatures(assayData = psm_tmt_total,
  quantCols = 36:45,
  name = "psms_raw")

message_parse(SummarizedExperiment::rowData(tmt_qf[["psms_raw"]]),
              "Master.Protein.Accessions",
              "Input")

}
