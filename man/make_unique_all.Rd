% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_unique_all.r
\name{make_unique_all}
\alias{make_unique_all}
\title{Make Elements of a Character Vector Unique (Numbering From 1)}
\usage{
make_unique_all(x, sep = ".", always = TRUE)
}
\arguments{
\item{x}{A character vector.}

\item{sep}{A character string to separate the original element
from the suffix. Defaults to \code{"."}.}

\item{always}{Logical. If \code{TRUE}, all elements (including singletons)
get a suffix (e.g. \code{"B"} becomes \code{"B.1"}). If \code{FALSE} (default),
singletons remain unchanged.}
}
\value{
A character vector of the same length as \code{x},
with duplicates disambiguated by appending a numeric suffix.
}
\description{
A variant of \code{\link[base:make.unique]{base::make.unique()}} that appends numeric suffixes
to elements of a character vector. Unlike \code{make.unique()}, which
leaves the first occurrence unchanged, this function can number
all members of a duplicated group starting from 1. Optionally,
it can number singletons as well.
}
\examples{
# Mimics make.unique() but numbers first duplicates as well
make_unique_all(c("A", "A", "B"))
# [1] "A.1" "A.2" "B"

# Multiple duplicate groups
make_unique_all(c("A", "A", "B", "B", "C"))
# [1] "A.1" "A.2" "B.1" "B.2" "C"

# Force suffixes on all elements
make_unique_all(c("A", "A", "B"), always = TRUE)
# [1] "A.1" "A.2" "B.1"

# Custom separator
make_unique_all(c("X", "X", "Y"), sep = "_")
# [1] "X_1" "X_2" "Y"

}
\seealso{
\code{\link[base:make.unique]{base::make.unique()}}
}
