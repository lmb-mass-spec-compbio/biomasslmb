% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maxlfq.r
\name{maxlfq_wrapper}
\alias{maxlfq_wrapper}
\title{Wrapper for iq::maxLFQ to use with QFeatures::aggregateFeatures}
\usage{
maxlfq_wrapper(mat, ...)
}
\arguments{
\item{mat}{A numeric matrix of peptide intensities (rows = peptides,
columns = samples). Missing values should be \code{NA}.}

\item{...}{Additional arguments passed to \code{iq::maxLFQ()}.}
}
\value{
A numeric vector of length equal to the number of columns in
\code{mat}, containing protein-level abundance estimates.
}
\description{
This function wraps \code{iq::maxLFQ()} so it can be passed to
\code{QFeatures::aggregateFeatures()} as the \code{fun} argument. It expects
a numeric matrix with peptides as rows and samples as columns, and
returns a numeric vector of protein-level intensities.
}
\details{
The MaxLFQ algorithm computes protein abundances from peptide
intensities by using peptide ratios across samples and solving a
least-squares system. This wrapper extracts the \code{estimate} component
returned by \code{iq::maxLFQ()} and coerces it to a numeric vector,
suitable for \code{aggregateFeatures()}.
}
\examples{
\dontrun{
library(QFeatures)
library(iq)

# Example peptide-level matrix (log2 intensities)
mat <- matrix(rnorm(20), nrow = 5, ncol = 4)
colnames(mat) <- paste0("Sample", 1:4)
rownames(mat) <- paste0("Pep", 1:5)

protein_estimates <- maxlfq_wrapper(mat)
}

}
