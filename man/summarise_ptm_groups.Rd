% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ptm_grouping.R
\name{summarise_ptm_groups}
\alias{summarise_ptm_groups}
\title{Summarise the PTM groups formed at a given min_candidate_prob}
\usage{
summarise_ptm_groups(
  obj,
  candidates,
  min_candidate_prob,
  min_prob = 0.501,
  max_group_span = 50,
  ...
)
}
\arguments{
\item{obj}{\code{SummarizedExperiment}. As passed to
\code{add_ambiguous_ptm_group_rowdata()}}

\item{candidates}{\code{data.frame} of candidate residues from one of the
\verb{parse_ptm_candidates_*()} functions}

\item{min_candidate_prob}{\code{numeric} Threshold to summarise}

\item{min_prob}{\code{numeric} Probability at or above which a residue counts as
localised}

\item{max_group_span}{\code{numeric} Widest span in residues a group may cover}

\item{...}{Further arguments for \code{add_ambiguous_ptm_group_rowdata()}}
}
\value{
One row \code{data.frame} of group size, span and coverage statistics
}
\description{
Runs \code{add_ambiguous_ptm_group_rowdata()} at one
\code{min_candidate_prob} and reports the size and span of the pooled groups it
forms, alongside how often those groups exclude the true site. Sweeping it
over a range of thresholds is how \code{min_candidate_prob} is chosen: raising it
buys smaller, more interpretable groups and pays in coverage.

The miss rate columns read directly off the probabilities. Because they sum
to one per modification, the mass a threshold leaves behind is the chance
that the group it forms does not contain the real residue, so
\code{pc_miss_over_5} is the percentage of pooled peptides whose group has more
than a 5\\% chance of excluding it.
}
