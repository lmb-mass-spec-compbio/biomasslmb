% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ptm_grouping.R
\name{parse_ptm_candidates_pd}
\alias{parse_ptm_candidates_pd}
\title{Parse Proteome Discoverer ptmRS site probabilities into candidate residues}
\usage{
parse_ptm_candidates_pd(obj, prob_col = "ptmRS.Phospho.Site.Probabilities")
}
\arguments{
\item{obj}{\code{SummarizedExperiment}. Proteomics dataset with PD PSM-level
rowData}

\item{prob_col}{\code{character} Column holding the site probabilities}
}
\value{
\code{data.frame} with one row per candidate residue and columns \code{row},
\code{pep_pos}, \code{residue}, \code{prob} and \code{n_ptms}
}
\description{
Extracts every candidate residue for a modification from the
ptmRS output, rather than only the residues that are confidently localised.

This reads \code{ptmRS.Phospho.Site.Probabilities}, \strong{not} the
\code{ptmRS.Best.Site.Probabilities} column that \code{parse_PTM_scores_pd()} uses. The
Best Site column reports only the winning isoform's sites, so the
alternatives a group is built from are absent from it. Pointing this function
at the Best Site column fails silently, producing groups that contain only
the sites ptmRS already preferred.

\if{html}{\out{<div class="sourceCode">}}\preformatted{Best Site Probabilities   : S3(Phospho): 39.61; S4(Phospho): 39.61
Phospho Site Probabilities: S(1): 1.3; S(3): 39.6; S(4): 39.6; S(6): 6.5
}\if{html}{\out{</div>}}

ptmRS reports percentages; they are rescaled to a 0 to 1 probability so that
\code{min_prob} and \code{min_candidate_prob} in \code{add_ambiguous_ptm_group_rowdata()}
mean the same thing whichever search engine produced the data. Note that
\code{parse_PTM_scores_pd()} takes its \code{threshold} as a percentage instead.

The number of PTMs is recovered from the total, which is 100 per
modification. Rows reading \verb{Inconclusive data} or \verb{Too many isoforms} match
nothing and so yield no candidates.
}
