% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uniparc_fallback.R
\name{get_uniprot_details}
\alias{get_uniprot_details}
\title{Get UniProt annotation details, with UniParc fallback for retired accessions}
\usage{
get_uniprot_details(accessions, verbosity = 0)
}
\arguments{
\item{accessions}{\verb{character vector} UniProt accessions.}

\item{verbosity}{\code{integer} Verbosity level for uniprotREST::uniprot_map.}
}
\value{
\code{data.frame} with one row per accession and columns \code{UniprotID},
\code{Entry.Name}, \code{Reviewed}, \code{Protein.names}, \code{Gene.Names}, \code{Organism},
\code{Length}, \code{Gene.Names.First}, \code{Annotation.Source}. \code{Annotation.Source} is
\code{"UniProtKB (live)"} for accessions resolved directly from UniProtKB, or
\code{"UniParc (retired UniProtKB entry)"} for accessions recovered via
\code{\link{get_uniparc_fallback}}. Accessions that are deleted and have no
recoverable UniParc annotation keep their original (blank/\code{"deleted"})
values from \code{uniprot_map()}.
}
\description{
Queries \code{uniprotREST::uniprot_map()} for gene/protein name
annotations for a vector of UniProt accessions, following the same
\code{uniprot_map()} usage as \code{\link{get_go_terms}}. Because MaxQuant
searches are run against a UniProt snapshot that may be weeks to years out
of date by the time annotation runs, some accessions may since have been
deleted from UniProtKB; these come back from \code{uniprot_map()} with blank
\code{Gene.Names} and \code{Protein.names == "deleted"}. For any such accession,
\code{\link{get_uniparc_fallback}} is used to recover the last-known
annotation from UniParc.
}
\examples{
\dontrun{
get_uniprot_details(c('O76024', 'Q03135', 'A0A5G2QPJ4'))
}
}
