% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync_coldata.R
\name{sync_coldata}
\alias{sync_coldata}
\title{Copy the experimental design onto one or more assays}
\usage{
sync_coldata(obj, i = names(obj))
}
\arguments{
\item{obj}{\code{QFeatures}. Proteomics dataset}

\item{i}{\code{character} or \code{numeric}. Assays to copy the \code{colData} onto.
Defaults to every assay in \code{obj}.}
}
\value{
\code{QFeatures} with \code{colData} attached to the named assays
}
\description{
\code{QFeatures::readQFeatures()} attaches \code{colData} at the
\code{QFeatures} level only, so the individual \code{SummarizedExperiment} assays
inside it start with an empty \code{colData}. Functions which plot or model a
single assay — \code{plot_quant()}, \code{plot_pca()}, \code{condition_miss_score()} and
anything using \code{limma} — need the design on the assay itself.

\code{sync_coldata()} copies the object-level \code{colData} onto the named assays,
matching on column name so that an assay holding a subset of the samples
gets the rows belonging to it.

Assays created by \code{QFeatures::aggregateFeatures()} and
\code{QFeatures::joinAssays()} also start without \code{colData}, so this is worth
calling after those as well as after reading the data in.
}
\examples{
tmt_qf <- QFeatures::readQFeatures(assayData = psm_tmt_clock,
  colData = tmt_clock_design,
  quantCols = rownames(tmt_clock_design),
  name = "psms_raw")

# the assay starts without the design attached
dim(SummarizedExperiment::colData(tmt_qf[["psms_raw"]]))

tmt_qf <- sync_coldata(tmt_qf)

dim(SummarizedExperiment::colData(tmt_qf[["psms_raw"]]))
}
