% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uniparc_fallback.R
\name{collapse_uniprot_details_multi_accession}
\alias{collapse_uniprot_details_multi_accession}
\title{Map per-accession UniProt details onto protein IDs with multiple accessions}
\usage{
collapse_uniprot_details_multi_accession(
  uniprot2details,
  protein_ids,
  sep = "; "
)
}
\arguments{
\item{uniprot2details}{\code{data.frame}, the output of
\code{\link{get_uniprot_details}}, covering (at least) every accession
that appears in \code{protein_ids} once split by \code{sep}.}

\item{protein_ids}{\verb{character vector} protein IDs, each optionally
containing several UniProt accessions joined by \code{sep}.}

\item{sep}{\code{string} delimiter separating accessions within a
\code{protein_ids} element. Default \code{"; "}, matching Proteome Discoverer's
\code{Master.Protein.Accessions} column.}
}
\value{
\code{data.frame} with one row per unique \code{protein_ids} element and
the same columns as \code{uniprot2details}, with \verb{;}-separated values where
the constituent accessions' details differ.
}
\description{
Takes the \code{data.frame} returned by
\code{\link{get_uniprot_details}} (one row per single accession) and a
vector of protein IDs where an element may itself be several accessions
joined by \code{sep} -- the \code{Master.Protein.Accessions} column from Proteome
Discoverer \code{PeptideGroups.txt} output is the common source of this. Each
\code{protein_ids} element is split into its constituent accessions, the
matching rows of \code{uniprot2details} are looked up, then re-collapsed back
to one row per original (possibly multi-accession) \code{protein_ids} element,
with \verb{;}-separated values where the constituent accessions' details
differ. Does not call \code{\link{get_uniprot_details}} itself, so the
same \code{uniprot2details} result can also be used on its own, without
fetching annotations twice.
}
\examples{
\dontrun{
uniprot2details <- get_uniprot_details(c('O76024', 'Q03135', 'A0A5G2QPJ4'))
collapse_uniprot_details_multi_accession(
  uniprot2details, c('O76024', 'Q03135; A0A5G2QPJ4'))
}
}
